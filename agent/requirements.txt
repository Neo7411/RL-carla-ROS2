# Explicitly fetch NVIDIA CUDA 11.8 enabled PyTorch wheels
--extra-index-url https://download.pytorch.org/whl/cu118

# Deep Learning Backend (Self-contained wheels to avoid MKL conflicts)
torch==2.0.1+cu118
torchvision==0.15.2+cu118
torchaudio==2.0.2+cu118

# Conflict-Free Pinned Packages
# Pinned to avoid Numpy 2.0 breaking changes and Pillow metadata errors
numpy<2.0.0
matplotlib<3.9.0
pillow<11.0.0
psutil<6.0.0
protobuf==3.20.2
jinja2>=3.1.2

# Reinforcement Learning & Simulation
gymnasium>=0.28.1
stable-baselines3[extra]>=2.1.0